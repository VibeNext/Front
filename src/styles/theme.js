const colors = {
  primary: '#3498db',
  gray: '#bdc3c7',
};

const fonts = {
  main: 'Arial, sans-serif',
  code: 'Courier New, monospace',
};

const theme = {
  colors,
  fonts,
};

export { theme };
